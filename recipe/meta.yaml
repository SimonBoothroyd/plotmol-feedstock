{% set name = "plotmol" %}
{% set version = "0.0.1" %}
{% set sha256 = "f8642f47f0f1c3c4df573ecfa60d751838106fb2de389bfcbfe4e6e2da9b5b47" %}

package:
  name: plotmol
  version: {{ version }}

source:
  url: https://github.com/SimonBoothroyd/{{ name }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv

requirements:
  host:
    - python
    - pip
  run:
    - python
    - rdkit
    - bokeh

test:
  imports:
    - plotmol

about:
  home: https://github.com/SimonBoothroyd/plotmol
  license: MIT
  license_file: LICENSE
  description: Interactive plotting of data annotated with molecule structures.

extra:
  recipe-maintainers:
    - simonboothroyd
